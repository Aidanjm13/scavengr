let jsonStatData;
let currTablePage = 0;
let maxTablePage = 0;

let myLineChart = null;

//test 3

$(document).ready(function() {
    $('#submitButton').click(function() {
        const textBox = document.getElementById("textBox");
        text = textBox.value;
        console.log(text);
        //do the loading symbol
        //use text to search for regex
        //display result
    });
});

